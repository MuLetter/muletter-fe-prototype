import * as auth from "./auth";
import * as info from "./info";
import * as mailbox from "./mailbox";
import * as map from "./mailboxMap";

const api = {
  auth,
  info,
  mailbox,
  map,
};

export default api;
